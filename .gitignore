# Secrets
.env
.env.*
!.env.example

# Node
app/node_modules/
app/dist/
app/coverage/
npm-debug.log*

# Generated vault index (rebuildable via `npm run index`)
JARVIS/INDEX/vault-index.json
JARVIS/INDEX/memory-index.json

# Logs
JARVIS/LOGS/*.log
app/*.log

# OS / editor
.DS_Store
*.swp

# Android build outputs / machine-specific SDK path (never the wrapper or
# the checked-in signing keystore — see JARVIS/ARCHITECTURE.md § Decisions)
android/local.properties
android/**/build/
android/.gradle/
